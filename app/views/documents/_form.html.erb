<%= form_for @document, url: documents_path do |f| %>
  <% if @document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@document.errors.count, "error") %> prohibited this document from being saved:</h2>

      <ul>
      <% @document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.select("tax", "tax_id", Tax.all.collect{|p| [ p.name, p.id]}, {include_blank: false})%>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field  :image %>
  </div>
  <div class="field">
    <%= f.label :per_from %><br>
    <%= f.date_select :per_from %>
  </div>
  <div class="field">
    <%= f.label :per_to %><br>
    <%= f.date_select :per_to %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
